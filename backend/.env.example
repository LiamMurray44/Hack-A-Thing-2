# ===================================
# FMLA Tracker Configuration
# ===================================

# Database Configuration
# -----------------------
# Development (SQLite - default, no server setup required):
DATABASE_URL=sqlite:///./data/fmla_tracker.db

# Production (PostgreSQL - uncomment and configure when ready):
# DATABASE_URL=postgresql://fmla_user:secure_password@localhost:5432/fmla_tracker
# DATABASE_URL=postgresql://user:pass@db.example.com:5432/fmla_prod

# Environment Settings
# --------------------
ENVIRONMENT=development  # development, staging, production
DEBUG=true               # Enable SQL logging and debug features

# Feature Flags
# -------------
USE_DATABASE=true        # Use database storage (false = use JSON files for rollback)

# API Configuration
# -----------------
API_TITLE=FMLA Deadline & Timeline Tracker
API_VERSION=0.2.0

# CORS Configuration
# ------------------
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
